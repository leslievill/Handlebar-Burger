<div class="flexbox-container">

        <div class="flexbox-item-1">            
            
            <ul id="uneaten">
                <h2> Devour Burger!!! </h2>
                {{#each burger}}
                {{#unless devoured}}
                {{> burger/burger-block devoured=false}}
                {{/unless}}
                {{/each}}
            </ul>

        </div>

        <div class="flexbox-item-2">
            
            <img src="http://pngimg.com/uploads/mcdonalds/mcdonalds_PNG3.png" alt="MDImg" id="burgerImg">
    
            <h2>Eat Da Burger!!!</h2>
        
            <form class="burgerInput">
            <input type="text" id="newBurger" name="burger_name">
                <div class="flexbox-container">
                    
                    <button type="submit" id="burgerBtn">Add Burger</button>

                </div>
            </form>
            
        </div>
        <div class="flexbox-item-3">
            
            <ul id="eaten">
                <h2>Burgers you have devoured: </h2>
                {{#each burger}}
                {{#if devoured}}
                {{!-- > means you're looking in the partials directory --}}
                {{> burger/burger-block devoured=true}}
                {{/if}}
                {{/each}}
            </ul>

        </div>
    </div>